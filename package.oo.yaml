version: 0.0.3
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
dependencies:
  {}
icon: ./icon.png
name: v2ex-lottery
description: V2EX Lottery
displayName: V2EX Lottery
repository: https://github.com/BlackHole1/oomol-v2ex-lottery
